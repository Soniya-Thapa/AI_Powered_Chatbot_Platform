# AI Powered Chatbot Platform – Backend

## About
This is the backend for the AI Powered Chatbot Platform built with **Node.js**, **Express**, and **TypeScript**.

## Getting Started

### Development
```bash
npm run dev
Runs the server in development mode with ts-node and nodemon. The server auto-reloads on file changes.

Production
bash
Copy code
npm run build
npm start
Compiles TypeScript to JavaScript and runs the compiled code from dist/.

Default Server
Open http://localhost:4000 in your browser.
You can edit server.ts; changes will auto-update in dev mode.

Scripts
Command	Description
npm run dev	Run server in development mode (TypeScript + auto-reload)
npm run build	Compile TypeScript to JavaScript
npm start	Run compiled JavaScript (production)

Dependencies
express → Web framework

Dev Dependencies
typescript → TypeScript compiler

ts-node → Run TypeScript directly

nodemon → Auto-reload server

@types/node → Node types

@types/express → Express types# AI_Powered_Chatbot_Platform






